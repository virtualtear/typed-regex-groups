/**
 * Builds a greeting for the given name.
 *
 * @remarks
 * Placeholder implementation shipped with the template so that build, test,
 * bench, and publish paths are exercised before any real code exists.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
